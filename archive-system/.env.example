# 环境变量模板 — 复制为 .env 后修改
# cp .env.example .env

APP_ENV=development
LLM_MODE=mock
OCR_MODE=mock
DB_MODE=sqlite
DB_PASSWORD=CHANGE_ME
MYSQL_ROOT_PASSWORD=CHANGE_ME
JWT_SECRET=CHANGE_ME_TO_RANDOM_STRING
