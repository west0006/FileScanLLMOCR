
## 二、争议点裁决（严格按《详细功能清单》）

| # | 争议点 | 方案书 | 用例 | **清单（基准）** | 裁决 |
|---|---|---|---|---|---|
| 1 | 检索导出格式 | PDF+Excel | — | **Excel/CSV**（SE-023） | 清单无 PDF；「选 Excel 下载 CSV」「PDF 500」均已修（fpdf2 已装），PDF 非必需 |
| 2 | 报表模板选择 | 有 | — | **无此概念** | 清单未要求，不属缺陷 |
| 3 | 预设角色 | 三角色 | 四角色 | **两角色**（系统管理员/档案馆员，UM-006） | 三份冲突，当前代码三角色，**待拍板** |
| 4 | 手动「锁定」按钮 | 有 | 锁定/解锁 | **启用/停用**（UM-004）+ 失败自动锁定（UM-005） | 清单无手动锁定，停用/启用即满足 |
| 5 | 「查档人员」角色 | 无 | 有 | **无**（两角色） | 清单无此角色，不属缺陷 |
| 6 | 日志保留期 | ≥3年 | 180天 | **未规定** | 代码已 1095 天(3年)；文案已统一（本轮修） |
| 7 | AI 预审含人工复核 | 三级人机协同 | 无需复核 | **仅 AI 预审**（RV 模块声明） | 清单为准，当前代码正确 |
| 8 | 开放建议枚举 | 三档 | 三档(异) | **四档**（RV-003） | 已按清单四档统一（8.13） |

---

## 三、按模块核对

### 模块一 智能检索

| Bug | 状态 |
|---|---|
| 档号 Q1995-DQ-012 搜不到 | 🟡 数据格式（seed 无 `Q` 前缀） |
| 排序完全未生效 | ✅ sortBy `@change="doSearch"` |
| 目录树部门节点显示为输入框 | ✅ 8.13 补二级部门节点 |
| 缺报表模板选择 | ⚠️ 清单无（争议#2） |
| 选 Excel 下载 CSV / PDF 500 | ✅ blob + fpdf2 2.8.7 |
| 内容摘要/关联档案/下载打印按钮 | ✅ 7.30 已修 |
| 开放状态/保管期限筛选 | ✅ 7.30 已补 |
| 检索结果导出失败 | ✅ 7.30 Pydantic+FileResponse |
| 筛选不生效/无标签 | ✅ 8.13 补筛选标签 |
| 层级「项目级」无结果 | 🟡 无 project 级数据 |
| 语义检索无结果 | 🟡 mock 不做语义（需 Ollama） |
| 归档年度只单选 | ⚠️ 清单 SE-012 是范围；8.3 已改 year_from/year_to |
| 缺多字段独立输入框 | ✅ 8.13 高级检索已补（SE-013） |

### 模块二 OCR

| Bug | 状态 |
|---|---|
| 创建任务缺目录树/全选/范围提示 | ⚠️ 清单 OC-001 要下拉范围（已有）；目录树非硬性 |
| 超50字符/同名任务 | ✅ max_length + 查重 + error 检查 |
| 进度 X/Y 非百分比 | ✅ 8.3 `X/Y (N%)` + 进度条 |
| 详情弹窗缺字段 | ✅ 8.3 补全 |
| 特殊字符未拦截 | ✅ 前端正则 |
| 缺优先级列/调整 | ✅ 7.24 + 8.13 |
| 「待处理」筛选空 | 🟡 创建即 running（状态流设计） |
| 缺暂停/恢复/取消 | ✅ 按状态条件渲染 |
| 无失败状态可模拟 | 🟡 需手动构造数据 |

### 模块三 AI 预审

| Bug | 状态 |
|---|---|
| 「建议延期开放或不予开放」筛空 | ✅ 8.13 四档 + 筛选值匹配 |
| 风险分布全 0 | ✅ seed 示例任务关联 task_id（旧库需 `seed(force=True)`） |
| 预审结果未归档 | ✅ `_sync_open_status` 批量回写 |
| 日期筛选无日期选择器 | ⚠️ 清单要「预审时间范围」，8.13 已补；日期选择器 UI 非硬性 |
| 按件半选/详情/案卷按钮 | ✅ 8.3 已补 |
| 蓝色提示框/筛选计数 | ✅ 8.3 已补 |
| 新建任务缺目录树/截止日期 | ✅ 截止日期已补；目录树下拉已补 |
| 导出只提示「任务已创建」 | ✅ 8.3 真实 API |
| 无页码分页 | ✅ 7.25 翻页 |
| AI 建议字体未变色 | ❌ UI 细节（低优先） |
| 双击案卷无响应 | ⚠️ 清单 SE-011 是「点击进详情」，双击非必需 |

### 模块四 数据同步

| Bug | 状态 |
|---|---|
| 状态卡片/双通道说明/保存提示 | ✅ 均已有 |
| 无「测试连接」按钮 | ⚠️ 有按钮但间接测试（清单 SY-003 未硬性要求） |
| 自动同步无「系统」日志 | ✅ `_log_sync_op` |

### 模块五 用户与权限（详见第四节专项）

| Bug | 状态 |
|---|---|
| 无筛选区/搜索框/最后登录列/部门/分页 | ✅ 全都有 |
| 账号已存在假成功 | ✅ 后端 error + 前端检查 |
| 无锁定按钮 | ⚠️ 清单用「停用」（争议#4） |

### 模块六 操作日志

| Bug | 状态 |
|---|---|
| 无四张统计卡 | ✅ |
| 结束日 15:30 漏数据 | ✅ date_to 补 23:59:59 |
| 「暂无日志」vs「暂无数据」文案 | ❌ 文案未统一（极低优先） |
| session_id 缺列 | ✅ 代码有 + 迁移补全 |
| 三分类 | ✅ tab + 统计卡 + typeLabel |
| 功能访问记录 admin/log | ✅ 8.12 补 map |
| 今日操作绑定历史总数 | ✅ today_total |
| 超50条无分页 | ✅ |
| 导出含范围外记录 | ✅ Batch 3 filters |
| reviewer 访问 200 | ✅ DEV_PERMISSIVE=false |
| 保留180天 vs 3年 | ✅ 行为 1095 + 文案已统一（本轮） |
| target_id 为空 | ✅ 七要素补全 |
| 链校验 tampered | ✅ 本会话修（6e07287 + 959f057） |
| 无每月自动审计报告 | 🟡 Celery Beat 已配，需 Redis |

### 模块七 统计

| Bug | 状态 |
|---|---|
| total vs count（只显用户名） | ✅ 后端返回 search/view_entry/view_file/download/print |
| 失败显示 0 | ✅ result=failure |
| 10万条多5000（失败重复计入） | ✅ 非 bug：by-type 已分离失败项，前端 total/failed 独立展示 |
| 空数据无「暂无数据」 | ⚠️ 有 chart-empty 但视觉占位 |
| 强制500显示853条虚构数据 | ✅ 8.3 移除 mock 回退 |
| 趋势图按月一条无名曲线 | ✅ 8.3 四线拆分 |
| 视口缩小不调整 | ⚠️ resize 监听存在，CSS 断点待验 |
| 本年累计/趋势前端固定规则 | ⚠️ 8.3 改 API 动态，趋势仍启发式（待验） |

---

## 四、在线用户 + 角色权限专项核对

### 4.1 在线用户

| Bug | 状态 | 依据 |
|---|---|---|
| 缺少查档人员卡片 | ✅ 有卡片；但后端无 searcher 角色（争议#5），恒 0 | OnlineUsers.vue |
| ip 显示 testclient | 🟡 测试环境 `client.host` 无真实 IP（非代码） | user.py list_online |
| 无「刷新」按钮 | ✅ 「手动刷新·30秒自动」卡片可点 | OnlineUsers.vue |
| 登录时间格式只有年月日 | ✅ UserManagement 已 `substring(0,19)` 含时分秒 | UserManagement.vue |
| 无会话时长列 | ⚠️ OnlineUsers 有 `sessionDuration`，但用 `last_login_at` 估算，非真实会话 | OnlineUsers.vue |
| 位置/办公区域 | ❌ 硬编码「档案馆办公区」 | OnlineUsers.vue |
| 当前操作页面 | ❌ 硬编码「在线用户页面」 | OnlineUsers.vue |
| 当前管理员标签 | ✅ `isSelf` →「当前管理员」tag | OnlineUsers.vue |
| 强制下线按钮 | ✅ 已移除；但强制下线功能未实现（清单无此功能点） | — |
| 停用用户仍可操作 | ✅ `get_current_user` 每请求检查 is_active → 403 | security.py |
| 管理员统计不正确 | ✅ `adminCount` 用 `includes('管理员')` 匹配正确 | OnlineUsers.vue |
| 点击卡片未筛选 | ✅ `statFilter` + `filteredUsers` | OnlineUsers.vue |
| 未授权访问防护 | ⚠️ `list_online_users` 仅 `get_current_user`，无 `require_role` | user.py |

### 4.2 角色权限

| Bug | 状态 | 依据 |
|---|---|---|
| 角色列表未显示权限 | ⚠️ 权限经「权限配置」按钮查看，列表内联不展示 | RoleManagement.vue |
| 每个角色权限无法查看 | ✅ 权限配置弹窗可看 | RoleManagement.vue |
| 新增角色「已存在」未提示 | ❌ 后端返回 `{"error"}`+200，前端 `createRole` 不检查 → 假成功 | user.py + RoleManagement.vue |
| 编辑权限不更新 | ✅ savePerm 后 `fetchRoles()` 刷新 | RoleManagement.vue |
| 目录树无全选按钮 | ⚠️ 权限弹窗无目录树（目录树授权在用户管理 tree_auth） | RoleManagement.vue |
| 案卷级/卷内级不可配置 | ❌ 权限弹窗仅功能权限（7 模块），无「案卷级条目浏览/卷内级浏览下载打印」数据权限粒度（UM-012/013） | RoleManagement.vue |
| 无删除选项 | ✅ deleteRole 按钮 + 端点（含用户数检查） | user.py + RoleManagement.vue |
| 内置角色删除保护 | ⚠️ 有 `user_count>0` 禁用，但无「内置角色不可删」硬保护 | user.py |
| 查档人员缺失 | ⚠️ 清单两角色（争议#5） | — |
| 密码复杂度校验 | ✅ reset_password 正则（大写/小写/数字/特殊字符） | user.py |
| 密码30天过期 | 🟡 生产模式才触发（非代码#4） | — |
| 5次失败锁定 | 🟡 生产模式才触发 | — |
| 会话超时30分钟 | 🟡 session_timeout 已实现，dev 不触发 | — |
| HTTPS/TLS | 🟡 环境（需 Nginx SSL） | — |
| 保存失败提示 | ⚠️ 非代码（方案书未规定固定文案，非代码#16） | — |
| 文件级/目录权限控制 | 🟡 数据权限未全链路（UM-012/013） | — |
| 接口级权限控制 | ✅ require_role/require_permission | security.py |
| 后端异常空状态 | ✅ fetchRoles catch → mock 回退 | RoleManagement.vue |

---

## 五、仍存在的真缺陷（建议下轮修）

| 优先级 | 项 | 说明 |
|---|---|---|
| P1 | 角色权限新增「假成功」 | `RoleManagement.createRole` 不检查 `res.data.error`（横切根因#4 未覆盖此页） |
| P1 | 在线用户接口越权 | `list_online_users` 仅 `get_current_user`，应加 `require_role` |
| P2 | 案卷级/卷内级数据权限配置缺失 | 权限弹窗仅功能权限，无 UM-012/013 数据权限粒度（较大 feature） |
| P2 | 位置/当前操作硬编码 | 需真实会话追踪（较大 feature） |
| P2 | 内置角色删除保护 | 删除 preset 角色应拒绝 |
| P3 | 「暂无日志」文案统一 / 空数据占位 / AI 建议字体变色 | UI 细节 |

---
