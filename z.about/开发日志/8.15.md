# 8.15 开发日志 — 全项目审查与修复汇总

> 本日志由「8.15 全项目审查报告」演进而来，汇总审查发现的缺陷修复落地情况。
> 审查方式：5 个隔离审查代理并行核查后端核心/服务/API/前端/训练部署，共发现 **~25 blocking + 40+ should-fix + 30+ nit**。

## 一、修复总览

按报告建议的 P0→P3 顺序修复，另追加 A/B/C/D 四批（正确性缺陷、配置、nit、需求口径对齐），并追加「残留处理」「全库安全复查」批次，累计 **22 个修复提交**。

| 批次 | 提交 | 范围 |
| --- | --- | --- |
| P0 安全 | `7aa34ad` `6546a46` `cf9ac98` | 数据权限、鉴权/提权、XSS |
| P1 数据一致性 | `0de8eca` `b5ed3ea` `44ab5f6` | MySQL 迁移、敏感词/LLM/GBK、契约 |
| P2 功能可用性 | `a75d0c6` `7f08811` | OCR 3.x、训练/脱敏、部署编排 |
| P3 体验/健壮性 | `19b351a` `4d37064` | 前端假数据/竞态/门控、配置安全 + nit |
| A 正确性缺陷 | `be37c23` `04d8c9a` `0510a7f` `e25ec4e` | open_status 落库、任务状态机、retry 幂等、登录 401 |
| B 配置/部署 | `9bd6a4c` | DATABASE_URL 密码编码、LLAMAFACTORY_URL 可配 |
| C nit | `a1af724` | 人名误报、PDF 字体、种子日志 user_id |
| D 需求口径 | `dd397e3` | 预设角色对齐清单 UM-006 |
| 残留处理 | `e13cfb5` `4a43e18` `e26c0fa` | QLoRA 配置、ECharts dispose、列表 fetch 序号、日志哈希链跨容器锁、竞态补漏 |
| 全库安全复查 | `fc8cb3b` `dc19009` | 13 项新问题（OCR 数据范围/任意文件读取、preview 越权、批量任务 admin 化、密码脱敏、JWT 角色、健康检查脱敏、生产关闭 docs 等） |

## 二、Blocking 25 项修复状态

### A. 数据权限越权（#1-10）

| # | 问题 | 状态 | 修复 |
| --- | --- | --- | --- |
| 1 | tree_auth 成对被拆成 `category OR department`（笛卡尔积越权） | ✅ | `apply_data_scope`/`_build_data_scope_filters` 改为成对 `(category AND department) OR` |
| 2 | ES 路径空授权=全量放行（与 SQLite 语义相反） | ✅ | ES 空授权改为匹配空集 `must_not match_all` |
| 3 | `has_data_permission` 空配置=全量放行 | ✅ | 改 fail-closed + seed 补 reviewer `data_permissions` |
| 4 | `update_user` 无角色检查（reviewer 可提权） | ✅ | 加 `require_role(system_admin)` |
| 5 | `serve_sync_file` 零鉴权（匿名可下载） | ✅ | 加鉴权 + 数据范围校验，前端改鉴权 blob 拉取 |
| 6 | 同步配置明文泄露（含源库密码） | ✅ | 配置接口加 `system_admin` 鉴权 |
| 7 | 任意文件读取链（改 share_paths → 复制 → 读出） | ✅ | share_paths 配置接口加 `system_admin` 鉴权 |
| 8-10 | OCR 全文/原文/预审导出无数据范围 | ✅ | `archive_ocr_text`/`archive_image`/`get_ocr_result`/`export_review_results` 补 `apply_data_scope` |

### B. 数据层 / 迁移（#11-12）

| # | 问题 | 状态 | 修复 |
| --- | --- | --- | --- |
| 11 | MySQL 无迁移机制（migrations/ 空） | ✅ | `_add_missing_columns` 方言通用化 + 启动无条件 `init_db()` |
| 12 | init_mysql.sql 与 models 字段漂移 20 列 | ✅ | 补全 users 2 / roles 1 / archives 9 / review_tasks 2 / ocr_tasks 3 / operation_logs 3 |

### C. 服务层（#13-16）

| # | 问题 | 状态 | 修复 |
| --- | --- | --- | --- |
| 13 | 敏感词库与官方敏感词.md 缺失 ~46 词 + 「特别行动队」错字 | ✅ | 补齐词条 + 修正错字 |
| 14 | LLM 故障时 `_fallback_result`=50 分污染干净档案 | ✅ | `llm_available=False`，`hybrid_review` 走纯规则评分 |
| 15 | ingest `errors="replace"` 使 GBK 回退永不执行 | ✅ | 去掉 `errors="replace"` |
| 16 | OCR real 模式与 paddleocr 3.x 不兼容 | ✅ | 迁移 3.x API（`device`/`predict`/`PPStructureV3` + 新返回结构） |

### D. 前端 XSS（#17-18）

| # | 问题 | 状态 | 修复 |
| --- | --- | --- | --- |
| 17 | SearchHome 高亮 XSS + 关键词裸拼正则崩溃 | ✅ | 先转义再高亮 + 正则转义 |
| 18 | ReviewWorkbench XSS + 高亮错位 | ✅ | title 属性转义 + 按原始 offset 占位再统一转义 |

### E. 训练 / 部署（#19-25）

| # | 问题 | 状态 | 修复 |
| --- | --- | --- | --- |
| 19 | lora_train.yaml fp16 3B 超 6GB 显存必 OOM（缺 QLoRA） | ❌ **未修** | 见「残留项」 |
| 20 | 训练样本自相矛盾（human_label 与 score/level/reason 冲突） | ✅ | `_label_consistent` 一致性映射 |
| 21 | desensitize.py `\b` 边界失效（中文语境零匹配） | ✅ | `(?<!\d)(?!\d)` 替代 `\b` |
| 22 | init_es.sh IK 插件缺失 | ✅ | 加 IK 前置检查 + 安装指引 |
| 23 | L3 编排断裂（arm.yml 缺失 + TARGETARCH + DB_MODE） | ✅ | 新建 `docker-compose.arm.yml` + 补 `DB_MODE=mysql` |
| 24 | ocr_deploy.sh `use_gpu=${STRATEGY}=="gpu"` NameError | ✅ | 改 `device='${DEVICE}'`（3.x API） |
| 25 | train.sh `tools/train.py` cwd 假设矛盾 | ✅ | 补 `PADDLEOCR_HOME` 检查 + `cd` |

## 三、should-fix / nit 修复状态

### 权限补强（✅ 全修）

- `list_users`/`get_user`/`get_tree_auth`/`list_roles` 加角色检查
- `update_ocr_task` 加归属校验
- `get_review_record`/`archive_knowledge_graph`/`search_facets` 补 `apply_data_scope`
- `archive_related` 核实为**误报**（已调用 `apply_data_scope`），未改动

### 契约不一致（✅ 全修）

- `risk_dist`(英文键) vs `risk_distribution`(中文键) → 统一 `risk_dist`
- ocr 列表 `id` vs create `task_id` → 统一补 `id`
- ES `risk_level` 取 open_status vs SQLite 硬编码「低」→ 统一「未审核」占位

### 竞态（⚠️ 大部分修复）

| 项 | 状态 |
| --- | --- |
| 任务状态可回退 / 重复派发双 worker | ✅ 状态机转换白名单 |
| 任务 retry 整批重跑无幂等 | ✅ 已审跳过 + 进度从已有记录初始化 |
| `pollProgress` 定时器无 onUnmounted 清理 | ✅ 补清理 |
| 前端多列表 fetch 无请求序号 | ✅ 5 个列表 + SearchHome + QueryStats + SyncManagement 统一加序号 |
| ECharts 实例重复 init 泄漏 | ✅ QueryStats + Home 均加实例跟踪 + `onUnmounted` dispose |

### 错误处理（✅ 全修）

- 前端多处 catch 塞假数据（Home 94.6%/趋势、ArchiveDetail 伪造示例、ReviewWorkbench MOCK 回退）→ 去除
- `_base.ts` 登录 401 也整页跳转 → 登录失败不再跳转
- `preview_review` 的 open_status 回写不 commit → 补 commit

### 前端门控（✅ 全修）

- ReviewTaskList 导出按钮未按 `can('review','export')` 门控 → 补门控
- router 无权限守卫 → 加 `meta.module` + `auth.can` 守卫
- MainLayout 权限接口失败显示全量菜单 → 失败时仅显示首页

### 配置安全（✅ 全修）

- `JWT_SECRET`/`DB_PASSWORD` 默认值 → 生产环境启动即拒绝
- 双 `.env` 分裂 → 清理遗留 `backend/.env`
- `DATABASE_URL` 密码未 URL 编码 → `quote_plus`
- `LLAMAFACTORY_URL` 硬编码 → 入 Settings 可配

### nit（✅ 大部分修复）

| 项 | 状态 |
| --- | --- |
| 中间件 CORS 顺序（跨域读不到错误体） | ✅ CORS 移到最外层 |
| session_timeout 未认证请求写内存 store（DoS） | ✅ 仅追踪有效 token |
| hash_password 72 字节截断碰撞 | ✅ 按字节截断 |
| entity_extractor 人名误报（「严肃」→人名） | ✅ 加常见非人名词黑名单 |
| PDF 中文导出容器无字体乱码 | ✅ backend Dockerfile 装 wqy 字体 |
| CSV 公式注入 | ✅ `= + - @` 开头前置单引号 |
| 日志哈希链跨容器分叉 | ✅ 线程锁 + 文件锁（同机）+ MySQL `FOR UPDATE`（跨容器） |
| 种子日志 user_id=1 与 username 矛盾 | ✅ 按 username 查 user_id |

## 四、验证结果

- 后端：`python -m pytest archive-system/backend/tests/ -q` → **107 passed**（每步后复跑无回归）
- 前端：`vite build` 成功（2275 模块）
- 脱敏实测：中文语境身份证/手机号正确匹配

> 注：前端 `npm run build` 中 `vue-tsc` 因 `vue-tsc@^1.8.0` 与 TS5/Node22 不兼容报错的问题，已在「八、代码质量收尾」中通过升级 `vue-tsc@2.2.0` 解决，`npm run build` 现已完整通过。

## 五、残留项

原 5 项残留已处理，最新状态：

| # | 项 | 状态 |
| --- | --- | --- |
| 1 | #19 lora_train.yaml fp16 3B OOM | ✅ 加 `quantization_bit: 4`（QLoRA），本地无法验证训练，需在 LLaMA-Factory 实测 |
| 2 | 前端多列表 fetch 请求序号 | ✅ 5 个列表 + SearchHome + QueryStats + SyncManagement 统一加序号 |
| 3 | ECharts 实例统一 dispose | ✅ Home.vue 加 `charts` 跟踪 + `onUnmounted` dispose |
| 4 | 日志哈希链跨容器分叉 | ✅ 线程锁 + 文件锁（同机）+ MySQL `FOR UPDATE`（跨容器） |
| 5 | entity 人名误报 | ⚠️ 已加黑名单（best-effort），彻底解决需接入真实 NER 模型，超出本代码库范围 |

> 注：#19 的 QLoRA 配置需在 LLaMA-Factory（向日葵 → 学校主机）上实测验证显存占用；#5 属 NER 模型能力范畴，非本代码库可闭环。

## 六、需求口径（按详细功能清单定调）

三方文档（技术方案书/详细功能清单/测试用例）冲突已按**详细功能清单**口径收敛：

| 冲突项 | 清单口径 | 落地 |
| --- | --- | --- |
| 开放建议枚举 | 四档（建议开放/部分开放/延期开放/不开放，RV-003） | 代码已四档，无需改 |
| 预设角色 | 系统管理员、档案馆员（UM-006） | `reviewer` 降为可删除的自定义角色 |
| 日志保留期 | LG 模块未规定 | 保持 3 年（方案书「≥3 年」口径） |
| 账号状态术语 | 启用/停用 + 锁定（UM-004/005） | 代码已符合 |

## 七、全库安全复查（第二轮）

在 20 项提交落地后，用 `explore` 子代理对整个后端代码库（非仅本批 diff）做安全/正确性复查，新发现 **13 项**问题，已全部处理（2 个提交 `fc8cb3b` + `dc19009`）。

| # | 级别 | 问题 | 状态 |
| --- | --- | --- | --- |
| 1 | 高危 | `GET /api/ocr/quality-report` 绕过数据范围（OCR 文本前 200 字跨部门泄露） | ✅ 加 `apply_data_scope` |
| 2 | 高危 | `POST /api/ocr/detect` `/debug/test` 的 `image_path` 任意文件读取 | ✅ 路径限定 SYNC_DATA_DIR + admin 鉴权 |
| 3 | 中危 | `POST /api/review/preview` 无数据范围（可篡改任意档案 open_status） | ✅ 加数据范围校验 |
| 4 | 中危 | reviewer 可触发全库批量预审/OCR 任务 | ✅ create 任务 admin 化 |
| 5 | 中危 | 数据库密码明文存储/回传 | ✅ `get_sync_configs` 密码脱敏 |
| 6 | 中危 | 预审任务 metrics 跨部门全库汇总泄露 | ✅ 非 admin 返回空 |
| 7 | 中危 | JWT 角色陈旧（降权不即时生效） | ✅ `require_role` 重查 DB 角色 |
| 8 | 低危 | `/api/health` 泄露 db_mode/llm_mode/ocr_mode/ES 集群 | ✅ 脱敏仅返回 status/version |
| 9 | 低危 | `archive_related` 中心档案 IDOR 存在性探测 | ✅ 加 `apply_data_scope` |
| 10 | 低危 | search 导出无权限门禁（与 download 不一致） | ✅ 加 `require_permission("search","download")` |
| 11 | 低危 | 跨用户任务/同步元数据可见 | ✅ 任务/同步端点 admin 化 |
| 12 | 低危 | admin 专属 SQL 拼接（sync_database_task） | ✅ 评估无提权路径（管理员持 DB 凭据），不修 |
| 13 | 低危 | 生产环境开放 `/docs` `/openapi.json` | ✅ 生产环境关闭 |

> 复查「未发现」项：命令注入（全库 `os.system/subprocess` 零匹配）、SQL 注入（全 ORM 参数化）、文件路径穿越（`serve_sync_file` 已有双重前缀校验）、密码/token 写日志（零匹配）。

## 八、代码质量收尾（第二轮）

在「七、全库安全复查」后追加一轮代码质量收尾，共 3 项，全部本地可推进、直接影响交付质量。

| # | 项 | 根因 | 修复 | 验证 |
| --- | --- | --- | --- | --- |
| 1 | `npm run build` 失败 | `vue-tsc@1.8.27` 与 TS5.9/Node22 不兼容（`supportedTSExtensions` patch 失败） | 升级 `vue-tsc@^2.2.0` + tsconfig 加 `skipLibCheck`/`noEmit`，修 19 处隐式 any / 事件签名 / null 类型错误 | `npm run build` 完整通过（vue-tsc + vite，2275 模块） |
| 2 | `review_raw.json` 字段全脏 | 原解析器 `_find_header_row` 找不到表头就 fallback 到标题行，把「表1…年度…」的「年度」误判为列名；且数据源用错了「工作任务表」（无审核结论列） | 重写 `parse_review_xls.py`：表头识别不再 fallback、列映射区分「档号/序号」、新增「开放标识→四档建议」「分类号→门类」映射；改用正确数据源「系统原始数据」案卷级 xls | 510 条记录、表头残留 0、65 条带审核结论（62 延期开放 + 3 开放） |
| 3 | mock LLM 词库与规则引擎不一致 | `_MOCK_SENSITIVE` 仅 10 词，规则引擎 400+ 词按 9 类组织，mock 下双引擎判定口径矛盾 | 移除 `_MOCK_SENSITIVE`，mock 复用 `review_service.scan_sensitive` | mock 现能命中「历史敏感/违法犯罪/个人隐私」分类，107 passed 零回归 |

**关键发现**：项 2 暴露原 `review_raw.json`（1454 条）是从**错误的「工作任务表」**解析的——那些表根本没有「审核结论」列。真正的审核结论在「系统原始数据」案卷级 xls 的「开放标识」列（延期开放/开放），门类在「分类号」列（DQ→党群/XZ→行政）。修复后训练数据源头才真正可用。

### 详细功能清单二次核对

代码质量收尾后，逐项核对《详细功能清单》79 项，结论：**72 项确已实现，7 个编号存在真实缺口（UM-009~011 为同一根因）**，另有 SE-004/007/008、OC-002/006、LG-010 等受数据/环境制约但代码完整（开发日志早前「79/79」把「后端 API 存在 + 前端有按钮」当成了「端到端可用」）。

| 编号 | 缺口 | 实际代码状态 |
| --- | --- | --- |
| SE-020 | 原文在线浏览（PDF） | `sync.py:214` 返回 `415 pdf_not_supported`，前端 `<iframe>` 嵌 PDF 分支实为断链 |
| SE-021 | OCR 文本对照浏览 | 对照图像侧是硬编码占位 `<div>图像区域</div>`，无真实图像 |
| SE-022 | 检索历史记录 | 前端走 `localStorage`，后端 `/search/history` 端点定义但前端未调用，双轨未贯通 |
| UM-009/010/011 | 目录树授权（配置/继承/查看） | 后端全套实现（`user.py:313/330` + `apply_data_scope`），前端无任何 tree-auth UI 入口 |
| LG-006 | 操作类型分类（打印） | 前端「打印证明」纯 `window.print()`，无后端 `/print` 端点，`print` 操作类型永不产生 |

> 环境制约（代码完整、非缺陷）：SE-004 box 级无数据可验证、SE-007 语义检索 mock 仅 `query.split()` 需 `LLM_MODE=ollama/real`、SE-008 需 pypdf/python-docx、OC-002/006 需 Redis/ES、LG-010 需 Celery Beat。
