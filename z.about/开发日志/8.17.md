# 8.17 开发日志 — 验收 bug 收尾 + 全模块终审

## 项目概况

| 项目 | 档案智能查询与开放审核系统 |
|------|--------------------------|
| 客户 | 中南财经政法大学 档案馆/校史馆 |
| 仓库 | https://github.com/west0006/FileScanLLMOCR.git |
| 阶段 | 验收 bug 收尾、分模块终审、状态定稿 |

---

## 一、本轮工作概览

承接 8.15 全项目审查，对验收提交的 bug 列表逐条核对并收尾。工作方式遵循**「业务逻辑设计依据《详细功能清单》优先，其次《技术方案书》，都没有则搁置讨论」**的口径规则（本轮新增并固化）。

产出：

- **3 个修复 commit**（`95c641b`、`482b168`、`a03f654`），工作区干净。
- 逐条核对验收 bug（19 条），可修的落地、违背清单的撤销、需讨论口径的搁置。
- 分模块终审，最终状态定稿（72/79，与 8.15 一致）。

---

## 二、修复清单

### commit 95c641b — 前端 4 项缺陷（7 文件 + 1 新文件）

| Bug | 清单依据 | 修复内容 |
|-----|---------|---------|
| bug 10 密码复杂度未提示 + 重置假成功 | UM-003 | `UserManagement.vue` 新增 `passwordComplexityError`（与后端 `_password_complexity_error` 逐条一致），`doResetPassword` 检查 `res.data.error`，抽公共函数到 `constants.ts` |
| bug 16 目录树范围越权显示 | UM-009 | `search_facets` 返回 `departments`，`SearchHome.vue` 的 `scopeTreeData` 从硬编码常量改为基于 facets 的 `computed`，无权限门类/部门不出现 |
| bug 11 自助改密入口缺失 | UM-003「用户可自行修改密码」 | 新增 `ChangePassword.vue`（调 `/user/me/password`）+ 路由 + 顶部入口 |
| bug 12 闲置 30 分钟不退出 | UM-005 | `MainLayout.vue` 加会话闲置检测（30 分钟无操作主动 logout，补齐「纯闲置无请求」场景） |

### commit 482b168 — 遗留格式化（无功能变更）

- `OnlineUsers.vue`、`OcrTaskList.vue`：纯 prettier 格式化 + 注释掉调试 `console.log`。

### commit a03f654 — Ollama 真实模式 500 与失败审计（2 文件）

| 缺陷 | 根因 | 修复 |
|------|------|------|
| 真实请求返回 500 | `hybrid_review` 对 LLM 返回无类型防护，qwen2.5:3b 返回字符串 `risk_score`/`sensitive_items`/`confidence` 时抛 TypeError/AttributeError | 加 `_safe_float` 转换 + `sensitive_items` 列表/字典校验（实测复现：字符串 `"85"` → `85.0` 不再崩） |
| 失败审计 total=0 | `log_middleware._log_exception_failure` 是模块级函数（首参误写 `self`），`dispatch` 却用 `self._log_exception_failure(...)` 调用 → 端点 500 时 AttributeError，失败日志不落库 | 去 `self` 改模块调用（实测：500 后 `result=failure` 日志正确落库） |

---

## 三、撤销与搁置

| Bug | 处理 | 依据 |
|-----|------|------|
| bug 17/18 OCR 状态流（待处理为空/操作列仅详情） | **撤销** | 曾改为「手动启动」，违背清单 OC-002「自动调度执行」；撤销后判定为无 Redis eager 模式的环境限制（生产接 Redis + Celery worker 后状态流正常），非代码缺陷 |
| bug 9 案卷级「条目浏览」权限无消费者 | **搁置讨论** | 清单 UM-012/013 有定义，但落地牵出权限模型改造（entry_view 与 `apply_data_scope` 关系）+ seed 无 box 级数据，需与需求方对齐后实现 |
| bug 2 会话时长口径 | **搁置讨论** | 清单 UM-005 只定义「闲置退出」，未定义「会话时长显示」是「登录至今」还是「最近活跃至今」；方案书亦无，按规则搁置 |
| bug 1/13/19 | 环境/需实测 | 用户管理 403（权限设计）、HTTPS（部署）、OCR 失败状态（测试数据） |

---

## 四、分模块终审状态

> 基线为 8.15 的 72/79（91.1%），本轮为「已 ✅ 功能点的健壮性增强 + 隐藏缺陷修复」，未改变 ✅/⚠️ 格局。

### 模块一 智能检索（20 ✅ / 3 ⚠️）

| 编号 | 状态 | 说明 |
|------|------|------|
| SE-002 指定目录节点检索 | ✅ | 本轮增强：目录树范围按数据权限过滤（facets 驱动） |
| SE-020 原文在线浏览 | ⚠️ | TIFF→PNG 转码 ✅；PDF 后端 415（pdf.js 未接入） |
| SE-021 OCR 文本对照 | ⚠️ | 三 tab 有；对照图像侧硬编码占位 |
| SE-022 检索历史 | ⚠️ | localStorage；后端 `/search/history` 未接前端 |
| 其余 19 项 | ✅ | — |

### 模块二 OCR（7/7 ✅）

- 全部 7 项 ✅。
- bug 17/18 撤销（违背 OC-002），OCR 状态流为 eager 模式环境限制。

### 模块三 AI 预审（13/13 ✅）

- 全部 13 项 ✅。
- 本轮增强：真实 LLM 非标准 JSON 防御，`LLM_MODE=ollama` 下预审稳定性提升。

### 模块四 数据同步（9/9 ✅）

- 全部 9 项 ✅，无变化。

### 模块五 用户权限（11 ✅ / 3 ⚠️）

| 编号 | 状态 | 说明 |
|------|------|------|
| UM-003 密码设置 | ✅ | 本轮增强：前端四类复杂度校验 + 假成功修复 + 自助改密入口（ChangePassword 页） |
| UM-005 登录安全控制 | ✅ | 本轮增强：前端闲置 30 分钟主动退出 |
| UM-009/010/011 目录树授权 | ⚠️ | 后端全套实现（tree_auth 持久化/继承/查询）；前端配置入口仍缺失（同一根因） |
| 其余 9 项 | ✅ | — |

### 模块六 操作日志（9 ✅ / 1 ⚠️）

| 编号 | 状态 | 说明 |
|------|------|------|
| LG-010 安全审计支持 | ✅ | 本轮修复：失败日志落库（`_log_exception_failure` 误用 self），500 后审计 total 正确 |
| LG-006 操作类型分类 | ⚠️ | 检索/浏览/下载/导出已分类；打印无后端端点 |
| 其余 8 项 | ✅ | — |

### 模块七 查询统计（3/3 ✅）

- 全部 3 项 ✅，无变化。

### 汇总

| 模块 | 总数 | ✅ | ⚠️ |
|------|------|----|----|
| 一 智能检索 | 23 | 20 | 3 |
| 二 OCR | 7 | 7 | 0 |
| 三 AI 预审 | 13 | 13 | 0 |
| 四 数据同步 | 9 | 9 | 0 |
| 五 用户权限 | 14 | 11 | 3 |
| 六 操作日志 | 10 | 9 | 1 |
| 七 查询统计 | 3 | 3 | 0 |
| **合计** | **79** | **72** | **7** |

**完成率 72/79（91.1%）**，与 8.15 一致。7 项 ⚠️ 中 UM-009/010/011 为同一根因，实际独立缺口 5 处。

---

## 五、测试结果

- 后端：`pytest test_review_service + test_api + test_e2e + test_ocr_client` 在 `APP_ENV=development + LLM_MODE=mock` 下全绿（exit 0）。
- 前端：`npm run build`（vue-tsc + vite，2278 模块）通过。
- 实测：Ollama 就绪后真实推理返回标准 JSON；字符串字段防御验证通过；模拟 500 后失败日志正确落库。

> ⚠️ 环境提示：当前 `archive-system/.env` 为 `APP_ENV=production + LLM_MODE=ollama`。直接跑 pytest 会因 admin 密码校验被锁定（423）而全挂，需 `set APP_ENV=development && set LLM_MODE=mock` 覆盖后测试。

---

## 六、待办（需与需求方对齐后推进）

1. **bug 9 案卷级条目浏览消费**：明确 entry_view 与数据范围权限的优先级关系 + 是否补 box 级 seed 数据。
2. **bug 2 会话时长口径**：确认「会话时长」是「登录至今」还是「最近活跃至今」，决定是否引入 `last_activity` 字段。
3. **SE-020 PDF 在线预览**：接入 pdf.js 渲染（当前后端 415）。
4. **SE-022 检索历史双轨贯通**：后端 `/search/history` 与前端 localStorage 统一。
5. **UM-009/010/011 前端配置入口**：目录树授权 UI（后端已就绪）。
6. **待外部资源**：LLaMA-Factory LoRA 微调（向日葵→学校主机）、PaddleOCR GPU 真机、ES 投产（IK 插件）、生产环境切换（MySQL/Redis）。
